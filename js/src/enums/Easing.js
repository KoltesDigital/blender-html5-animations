'use strict';

// GRAPH_OT_easing_type

/**
 * FCurve easing types.
 * @enum {number}
 * @readonly
*/
var Easing = {
	/** Automatic easing. */
	AUTO: 0,
	/** Starts slow. */
	IN: 1,
	/** Ends slow. */
	OUT: 2,
	/** Starts and ends slow. */
	IN_OUT: 3,
};

module.exports = Easing;
